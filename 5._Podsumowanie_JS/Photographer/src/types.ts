export interface MappedValues {
    author: string;
    url: string;
}

export interface ReturnedValue {
    author: string;
    url: string;
    download_url: string;
    id: string;
    width: number;
    height: number;
}
